// 클래스 생성
//프론트 멤버

//1. 클래스 정의 (이름, 번호)
class Front {
    constructor(name, number) {
        this.name = name,
        this,number= number,
    }
}

//2. 클래스를 활용한 인스턴스

const 준규 = new Front("준규", 1);



console.log(준규)